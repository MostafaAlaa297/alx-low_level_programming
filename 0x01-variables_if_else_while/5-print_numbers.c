#include <stdio.h>
#include <ctype.h>

/**
 * main - Entry Point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{

	unsigned long i;

	for (i = 0; i < 10; i++)
	{
		printf(i);
	}

	printf('\n');

	return (0);
}
